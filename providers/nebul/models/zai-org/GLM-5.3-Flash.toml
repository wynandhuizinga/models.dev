# Efforts: reasoning_effort = low|high|max on POST /v1/chat/completions (verified live 2026-09-09)
# Traces arrive in message.reasoning (Nebul docs), so interleaved = true
base_model = "zhipuai/glm-5.3-flash"

interleaved = true

[[reasoning_options]]
type = "effort"
values = ["low", "high", "max"]

[cost]
input = 0.21
output = 0.63

[limit]
context = 1_048_572
